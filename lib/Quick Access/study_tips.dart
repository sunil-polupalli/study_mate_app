import 'package:flutter/material.dart';

class StudyTips extends StatelessWidget {
  const StudyTips({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
